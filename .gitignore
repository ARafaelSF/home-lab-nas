# Segredos — nunca commitar
.env
*.env
!*.env.example
# ficheiros de tokens gerados pelo influxdb-setup.sh (.env.tokens.new, etc.)
.env.tokens*
**/.env.tokens*
stack.env
stack.env.bak
**/stack.env
wud-registries.env
compose/dozzle/users.yml
*.password
**/.dashboard-password
compose/hermes-agent/.dashboard-password
**/secrets/
homeassistant/localthings-certs/

# Backups locais (podem conter dados sensíveis)
backups/*.tar.gz
backups/*.db
backups/manifests/runtime-*.txt
backups/influx-migration/

# Cursor / IDE
.cursor/
.cursor-server/

# OS
.DS_Store
*~

# UniFi MCP secrets
compose/unifi-mcp/.env
