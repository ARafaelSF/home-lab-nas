[Resolve]
# Servidor NAS: DNS directo (não via AdGuard) — evita loop e EAI_AGAIN no Docker/WUD
# Instalar: mkdir -p /etc/systemd/resolved.conf.d && cp homelab-dns.conf /etc/systemd/resolved.conf.d/ && systemctl restart systemd-resolved
DNS=8.8.8.8 8.8.4.4
