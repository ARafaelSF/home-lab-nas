# Tag da imagem (digest via WUD)
DOZZLE_TAG=latest

# Credenciais Dozzle (auth simple)
# Gerar users.yml:
#   docker run --rm amir20/dozzle:latest generate admin \
#     --password 'SUA_SENHA' --name "Antonio" --email "arafaelsf@gmail.com" > users.yml
