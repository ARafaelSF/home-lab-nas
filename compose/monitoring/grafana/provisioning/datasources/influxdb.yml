apiVersion: 1

datasources:
  - name: HomeAssistant
    uid: PBB7EDB19FB19A0A4
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: false
    editable: false
    jsonData:
      version: Flux
      organization: homelab
      defaultBucket: home_energy
    secureJsonData:
      token: ${INFLUXDB_GRAFANA_READ_TOKEN}
